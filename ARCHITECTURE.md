# MBM Connect - Architecture & Features Overview

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                          Frontend (Vite)                         │
│                      http://localhost:3001                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                     DarkModeContext                      │   │
│  │  ┌────────────────────────────────────────────────────┐ │   │
│  │  │ isDarkMode: boolean                                │ │   │
│  │  │ toggleDarkMode(): void                             │ │   │
│  │  │ LocalStorage: 'darkMode'                           │ │   │
│  │  └────────────────────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────┘   │
│                              ▲                                    │
│                              │ useContext                         │
│                              ▼                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                   App Components                         │   │
│  │  ┌──────────────────────────────────────────────────┐   │   │
│  │  │ Navbar (Dark Mode Toggle ☀️🌙)                 │   │   │
│  │  │ HomePage (Dark Gradients)                        │   │   │
│  │  │ LoginPage (Dark Forms)                           │   │   │
│  │  │ UserProfile (Profile Stats, Follow, Edit)        │   │   │
│  │  │ Dashboard, Market, Stories, Chat                 │   │   │
│  │  └──────────────────────────────────────────────────┘   │   │
│  └──────────────────────────────────────────────────────────┘   │
│                              ▲                                    │
│                              │                                    │
│                          HTTP/Axios                              │
│                              │                                    │
└──────────────────────────────┼────────────────────────────────────┘
                               │
                    ┌──────────▼──────────┐
                    │   API Base URLs    │
                    │  localhost:8080    │
                    └────────────────────┘
                               │
┌──────────────────────────────┼────────────────────────────────────┐
│                          Backend (Node.js)                        │
│                      http://localhost:8080                        │
├──────────────────────────────┼────────────────────────────────────┤
│                              │                                    │
│  ┌──────────────────────────▼──────────────────────────────────┐ │
│  │                   API Routes                                │ │
│  │  ┌─────────────────────────────────────────────────────┐  │ │
│  │  │ Auth Routes:                                        │  │ │
│  │  │ POST /api/auth/register                             │  │ │
│  │  │ POST /api/auth/login                                │  │ │
│  │  │ POST /api/auth/logout                               │  │ │
│  │  │ POST /api/auth/refresh                              │  │ │
│  │  └─────────────────────────────────────────────────────┘  │ │
│  │  ┌─────────────────────────────────────────────────────┐  │ │
│  │  │ User/Profile Routes (NEW):                          │  │ │
│  │  │ GET  /api/user/:id              (Get Profile)       │  │ │
│  │  │ PUT  /api/user/:id/profile      (Update Profile)    │  │ │
│  │  │ POST /api/user/:id/follow       (Follow User)       │  │ │
│  │  │ POST /api/user/:id/unfollow     (Unfollow User)     │  │ │
│  │  │ GET  /api/user/:id/posts        (Get User Posts)    │  │ │
│  │  └─────────────────────────────────────────────────────┘  │ │
│  │  ┌─────────────────────────────────────────────────────┐  │ │
│  │  │ Other Routes: Chat, Market, Stories, etc.           │  │ │
│  │  └─────────────────────────────────────────────────────┘  │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                              ▲                                    │
│                              │                                    │
│                         Mongoose                                  │
│                              │                                    │
│  ┌──────────────────────────▼──────────────────────────────────┐ │
│  │                   MongoDB Models                            │ │
│  │  ┌─────────────────────────────────────────────────────┐  │ │
│  │  │ User Model (Enhanced):                              │  │ │
│  │  │ ├─ fullName: String                                 │  │ │
│  │  │ ├─ username: String (unique)                        │  │ │
│  │  │ ├─ email: String (unique)                           │  │ │
│  │  │ ├─ password: String (hashed)                        │  │ │
│  │  │ ├─ avatar: String (Cloudinary URL)                  │  │ │
│  │  │ ├─ coverImage: String (Cloudinary URL)              │  │ │
│  │  │ ├─ bio: String (NEW)                                │  │ │
│  │  │ ├─ website: String (NEW)                            │  │ │
│  │  │ ├─ followers: [ObjectId] (NEW)                      │  │ │
│  │  │ ├─ following: [ObjectId] (NEW)                      │  │ │
│  │  │ ├─ postsCount: Number (NEW)                         │  │ │
│  │  │ ├─ refreshToken: String                             │  │ │
│  │  │ └─ timestamps: { createdAt, updatedAt }             │  │ │
│  │  └─────────────────────────────────────────────────────┘  │ │
│  │  ┌─────────────────────────────────────────────────────┐  │ │
│  │  │ Other Models: Post, Chat, Market, etc.              │  │ │
│  │  └─────────────────────────────────────────────────────┘  │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                              ▲                                    │
│                              │                                    │
│                        Connection                                │
│                              │                                    │
└──────────────────────────────┼────────────────────────────────────┘
                               │
                    ┌──────────▼──────────┐
                    │    MongoDB Atlas    │
                    │   (Cloud Database)  │
                    └────────────────────┘
```

## Feature Implementation Map

### 1. Dark Mode Flow

```
User clicks 🌙 Toggle Button
        │
        ▼
toggleDarkMode() called
        │
        ▼
isDarkMode state flips (true/false)
        │
        ├─▶ Update localStorage ('darkMode')
        │
        └─▶ Update document.documentElement.classList
            ├─ Add 'dark' class (if isDarkMode = true)
            └─ Remove 'dark' class (if isDarkMode = false)
        │
        ▼
All Components with dark: prefix re-render
        │
        ├─▶ Background colors change
        ├─▶ Text colors change
        ├─▶ Border colors change
        └─▶ Accent colors change
        │
        ▼
User sees theme switch ✨
```

### 2. Profile System Flow

```
┌─ User Flow ─────────────────────────────────────────┐
│                                                      │
│  User Visits Profile (/profile/:userId)             │
│         │                                            │
│         ▼                                            │
│  UserProfile Component Loads                        │
│         │                                            │
│         ├─▶ GET /api/user/:userId                  │
│         │   └─ Returns: User data + followers[]     │
│         │                        + following[]      │
│         │                                            │
│         ▼                                            │
│  Profile Rendered:                                  │
│  ├─ Cover Image (placeholder)                       │
│  ├─ Avatar + Stats                                  │
│  │  ├─ Posts: postsCount                           │
│  │  ├─ Followers: followers.length                 │
│  │  └─ Following: following.length                 │
│  ├─ Bio Section (editable if own profile)          │
│  └─ Posts Grid (placeholder)                       │
│         │                                            │
│         ▼                                            │
│  User Can:                                          │
│  ├─ Follow/Unfollow (if not own profile)           │
│  │  └─ POST /api/user/:id/follow                   │
│  │  └─ POST /api/user/:id/unfollow                 │
│  │                                                  │
│  └─ Edit Profile (if own profile)                  │
│     ├─ Update Form                                  │
│     └─ PUT /api/user/:id/profile                   │
│                                                      │
└─────────────────────────────────────────────────────┘

Follow Action Flow:
┌─ Following ─────────────────────────────────────┐
│                                                  │
│ User clicks Follow Button                       │
│     │                                            │
│     ▼                                            │
│ POST /api/user/targetId/follow                 │
│     │ (with Auth Token)                         │
│     │                                            │
│     ▼                                            │
│ Backend:                                         │
│ ├─ Add targetId to currentUser.following        │
│ ├─ Add currentUserId to target.followers        │
│ └─ Save both documents                          │
│     │                                            │
│     ▼                                            │
│ Response: Success                               │
│     │                                            │
│     ▼                                            │
│ Frontend:                                        │
│ ├─ Update isFollowing state                     │
│ ├─ Refresh profile data                         │
│ └─ Button changes to "Following" ✓              │
│                                                  │
└─────────────────────────────────────────────────┘
```

## Component Structure

```
App
├─ DarkModeProvider (wraps entire app)
│  └─ AuthProvider
│     ├─ Navbar
│     │  ├─ Logo (Instagram gradient)
│     │  ├─ Navigation Links
│     │  ├─ Dark Mode Toggle ☀️/🌙
│     │  └─ User Menu
│     │
│     └─ Routes
│        ├─ Public Routes
│        │  ├─ / (Home)
│        │  ├─ /home (HomePage)
│        │  ├─ /login (LoginPage)
│        │  ├─ /signup (SignupPage)
│        │  └─ /profile/:userId (UserProfile) ✨ NEW
│        │
│        └─ Protected Routes
│           ├─ /dashboard
│           ├─ /market
│           ├─ /chat
│           └─ /stories
```

## Data Relationships

```
User Document:
┌─────────────────────────────────────┐
│           User ID: 123              │
├─────────────────────────────────────┤
│ name: "John Doe"                    │
│ username: "johndoe"                 │
│ avatar: URL (Cloudinary)            │
│ bio: "Developer & Designer"         │
│ website: "https://johndoe.com"      │
│                                      │
│ followers: [                         │
│   { ref: User, id: 456 },           │
│   { ref: User, id: 789 }            │
│ ]  // Users following this user     │
│                                      │
│ following: [                         │
│   { ref: User, id: 234 },           │
│   { ref: User, id: 567 }            │
│ ]  // Users this user is following  │
│                                      │
│ postsCount: 15                       │
│                                      │
└─────────────────────────────────────┘
              ▲
              │ Relationships
              │
    ┌─────────┼─────────┐
    │         │         │
    ▼         ▼         ▼
  User 456  User 789  User 234
 (follower) (follower) (following)
```

## Data Flow Diagram

```
Browser Local Storage
├─ darkMode: true/false
├─ token: JWT_TOKEN
└─ user: { _id, username, email, avatar, ... }
              │
              ▼
    Components/Pages
    ├─ useDarkMode() → isDarkMode state
    ├─ useAuth() → user data, login, logout
    ├─ API calls via Axios
    │   ├─ Authorization header: Bearer TOKEN
    │   ├─ Content-Type: application/json or multipart/form-data
    │   └─ Interceptors: Auto token injection, error handling
    │
    └─▶ Backend API
        ├─ Auth Middleware checks token validity
        ├─ Process request
        ├─ MongoDB query/update
        └─ Return JSON response
        │
        └─▶ Frontend
            ├─ Update state
            ├─ Re-render components
            └─ Display to user
```

## Security Flow

```
Authentication:
┌─ Register/Login ────────────────────────────────┐
│                                                  │
│ 1. User submits credentials                     │
│    └─ bcrypt.hash(password)                    │
│                                                  │
│ 2. Backend creates JWT token                    │
│    └─ Contains: { userId, email, iat, exp }    │
│                                                  │
│ 3. Token sent to frontend                       │
│    └─ localStorage.setItem('token', token)     │
│                                                  │
│ 4. All subsequent requests include token        │
│    ├─ Authorization: Bearer <token>            │
│    └─ Auto-injected by Axios interceptor       │
│                                                  │
│ 5. Backend validates token                      │
│    ├─ Check signature validity                  │
│    ├─ Check expiration                         │
│    └─ Extract userId                           │
│                                                  │
│ 6. Protected routes only accessible to auth'd  │
│    users                                        │
│                                                  │
└─────────────────────────────────────────────────┘
```

## Styling System

```
Tailwind CSS Configuration:
├─ Colors
│  ├─ primary: Blue (0EA5E9)
│  │  └─ Shades: 50-900
│  ├─ secondary: Gray (64748B)
│  │  └─ Shades: 50-900
│  └─ accent: Amber (F59E0B)
│
├─ Dark Mode
│  ├─ enabled: 'class' based
│  ├─ Colors: secondary-900 (bg), gray-300 (text)
│  └─ Usage: dark:bg-secondary-900 dark:text-white
│
├─ Gradients
│  ├─ gradient-primary: Blue to Blue
│  ├─ gradient-dark: Dark gradient
│  └─ gradient-instagram: Multi-color (Instagram style)
│
├─ Animations
│  ├─ fade-in: Fade in animation
│  ├─ slide-up/down: Slide animations
│  └─ pulse-light: Pulsing animation
│
└─ Custom Utilities
   ├─ input-field: Standard input styling
   ├─ btn-primary: Primary button styling
   ├─ btn-secondary: Secondary button styling
   └─ card: Card component styling
```

---

**Status:** ✅ Complete and Ready for Use

The MBM Connect platform now features a professional Instagram-like design with:
- Dark mode support across all pages
- User profile system with follow/unfollow
- Profile editing capabilities
- Stats tracking (followers, following, posts)
- Professional color scheme and animations
